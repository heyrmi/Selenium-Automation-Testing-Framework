package enums;

/**
 * These are enums used to limit the number options user is allowed to used, thus minimizing errors
 */
public enum CategoryType {
	REGRESSION,
	SMOKE,
	SANITY,
	MINIREGRESSION;
}
